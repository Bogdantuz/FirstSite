document.querySelector("#download-now button").addEventListener("click", () => {
    alert("Sorry, but this app isn't already done")
})
document.querySelector("#watch-more button").addEventListener("click", () => {
    alert("Sorry, but this protocol doesn't already exist")
})
document.querySelector("#end-download-now button").addEventListener("click", () => {
    alert("Sorry, but this app isn't already done")
})